import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'F1 Champions',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
